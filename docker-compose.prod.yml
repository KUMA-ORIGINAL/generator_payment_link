
services:
  backend:
    build:
      context: backend
      dockerfile: Dockerfile.prod
    env_file:
      - .env.prod
    restart: always
    expose:
      - 8000
    volumes:
      - ./backend/app:/app
      - static_volume:/app/static

  health-checker:
    build:
      context: ./checker
    container_name: checker
    restart: on-failure:5
    volumes:
      - ./checker:/app
    env_file:
      - ./checker/.env
    depends_on:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f checker.py || exit 1"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

  nginx:
    build: nginx
    env_file: .env.prod
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - static_volume:/app/static
      - certs:/etc/letsencrypt
    depends_on:
      - backend

  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: mosquitto
    restart: always
    ports:
      - "1883:1883"
      - "8883:8883"              # TLS порт
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
      - certs:/etc/letsencrypt:ro

volumes:
  static_volume:
  certs:
